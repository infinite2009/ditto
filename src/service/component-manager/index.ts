// 若需要使用旧版组件管理器，请取消注释以下行
// import { ComponentManager } from './ComponentManager';
import {ComponentManager} from './ComponentManager.next';

export default ComponentManager;

export { generateDefaultStyleConfig } from './utils';
